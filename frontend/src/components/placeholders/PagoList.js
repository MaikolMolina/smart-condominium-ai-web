import React from 'react';
import Placeholder from './Placeholder';

const PagoList = () => {
  return (
    <Placeholder
      titulo="Realizar Pago de Cuotas"
      descripcion="Este módulo permitirá a los residentes realizar pagos en línea de cuotas y servicios mediante integración con pasarelas de pago."
      paquete="Finanzas & Cobranza"
    />
  );
};

export default PagoList;